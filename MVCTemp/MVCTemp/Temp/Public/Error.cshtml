@{
    Layout = null;
}
<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <style>
        .error {
            background: url(../../Content/images/eorr.jpg) no-repeat;
            height: 250px;
            width: 450px;
            margin: 50px auto;
        }

            .error div {
                padding-left: 120px;
                padding-top: 35px;
                font-family: "微软雅黑";
                font-size: 14px;
            }

                .error div b {
                    font-size: 32px;
                    color: #42D9FF;
                    font-weight: normal;
                }

                .error div em {
                    font-size: 10px;
                    color: #999;
                    font-style: normal;
                }

        .btn-submit {
            background-color: #1B66F7;
            color: #FFF;
            border: none;
            font-size: 14px;
            font-weight: bold;
            cursor: pointer;
            padding: 7px 12px;
            border-radius: 4px;
            -moz-border-radius: 4px;
            border: 1px solid #1B66F7;
        }

            .btn-submit:hover {
                background-color: #0091ce;
            }
    </style>
    <title>出错啦！</title>
</head>
<body>
    <div class="error">
        <div>
            <b>风速过大，出错啦！</b><br />
            <span>错误信息： @Html.Raw(ViewData["Error"])</span><br />
            <em>@Html.Raw(ViewData["ExceptionID"])</em><br />
            <em>@Html.Raw(ViewData["TrackID"])</em><br />
            <br />
            <span>
                <input name="" type="button" onclick="returnUrl();" class="btn-submit" value="返  回"></span>
        </div>

    </div>
</body>
</html>
<script type="text/javascript">
    function returnUrl() {
        history.back(-1);
    }

</script>
